# Project MCP for Codex (Astra / Sol). Loaded when this repo is trusted.
# Same AgentCAD tools Claude Code got via `claude mcp add agentcad`.

[mcp_servers.agentcad]
command = "cmd.exe"
args = ["/d", "/c", "scripts\\stella\\mcp.cmd"]
startup_timeout_sec = 300.0
tool_timeout_sec = 600.0

[mcp_servers.agentcad.env]
AGENTCAD_AGENT_ID = "codex"
