# 作者作成の試験片 — 元Req2CADデータではありません

3件の形状/機能注釈はテスト用に作成しました。UID 9000/90000001〜03もテスト用です。
意味検索モデルや元データは使っていません。機能語の字句検索→UID対応→本物のCADカーネル
→STEP/STL/4方向SVG→面トポロジー/幾何比較の経路を試します。耐久・摩擦・実用性は未検証。

`fixture_report.json` と `kb/geometry/*/` に実行結果を同梱しました。
実行環境の絶対パスは別PCで無効です。運用前に `scripts/demo_req2cad.py` を再実行してください。
本番ライブラリへこのSQLiteをコピーして「元データ取込済み」にしないでください。
