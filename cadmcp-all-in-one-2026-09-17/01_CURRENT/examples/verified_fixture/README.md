# Software verification fixture — NOT A MOUSE DESIGN

The two STEP files are deliberately simple, separate cuboid test solids. They do not
implement the side-button mechanism described by the authored IR. They exercise
the geometry measurement interface only. The manual and physical checks therefore
remain UNKNOWN. All dimensions are synthetic fixtures, not printer settings.
