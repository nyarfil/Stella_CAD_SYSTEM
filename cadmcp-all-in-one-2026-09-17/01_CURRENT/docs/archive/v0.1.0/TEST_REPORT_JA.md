# 動作検証報告 — cadmcp-design-brain v0.1.0

記録日: **2026-09-16**。検査対象はこのZIPの実装です。ユーザーの既存cadMCPへの組込み完了、任意設計の正解率、実物の性能保証ではありません。

## 結果

**141件合格 / 0件失敗 / 0件エラー / 0件skip。**

| 試験群 | 合格 | 失敗・エラー | skip |
|---|---:|---:|---:|
| 要求・構造・状態・MCP通信・設定・AgentCAD HTTP契約 | 128 | 0 | 0 |
| STEP実測・欠測・改変・測定時競合・未検証項目 | 13 | 0 | 0 |
| 合計 | 141 | 0 | 0 |

根拠は`verification/core-tests.xml`、`geometry-tests.xml`と対応する実行ログです。両群は同じ最終ソースに対し分けて実行しました。テスト総数をLLMの設計成功件数・認識正解率に読み替えないでください。

## 実際に検査した内容

原文の引用捏造・対応漏れ、推測の必須化、希望の強制条件化、数値型・単位・矛盾、構造案の重複、機能と機構の不一致、直押し方向の不整合、復帰・ストッパー・ガイドの不足、部品接続の不整合、部品数の自己申告偽装、作業依存の循環、検証義務の欠落、数値根拠の捏造を検査しました。

SQLiteの保存・再読込み、更新競合とロールバック、原文変更後の下流無効化、設計契約の再出力と改変検出、STEPのhash検査、古い契約の成果物の拒否、パス逸脱・symlink・不適切ファイルの拒否を検査しました。

**実プロセスのMCP stdio試験**では初期化、ツール呼出し、原文から用意済みIRの投入、計画、契約出力、未測定の検証状態まで通しました。通信内容は実際のJSON-RPCです。公式MCP SDKの検査器や、Cursor/Codexアプリ本体での相互接続試験ではありません。

**AgentCADアダプター試験**はローカルHTTPテストサーバーで行いました。実スキーマに基づく引数検査、optional null、HTTP 200のエラー本文、ツール許可リスト、redirect拒否、タイムアウト、同じcall_idの再送抑止、能力確認中の設計変更を検査しました。実AgentCAD本体の起動・書込みを確認したものではありません。

**設定ファイル試験**は他のMCP項目保持、元ファイルのバックアップ、冪等なマージ、不正JSONを上書きしない動作、空白／日本語パス、Codex TOMLの構文を確認しました。Windows用setup.cmd自体とWindowsでのpip installは実行していません。

## 実STEPの測定結果

対象は、CADカーネルで作成した**2個の直方体試験片**です。マウスの機構を完成させた造形モデルではありません。

| 項目 | 実測・判定 |
|---|---|
| B-repの有効性 | true / pass |
| frame試験片のX幅 | 20.0 mm / pass |
| 2試験片の最短距離 | 1.0 mm / pass |
| 干渉体積 | 0.0 mm³ / pass |
| 形状検査集計 | pass |
| 設計手動レビュー | unknown |
| 実物の耐久性 | unknown |
| 全体 | **unknown** |

これらの寸法・0.5 mmという試験用判定値は、実マウスや特定プリンタへの推奨値ではありません。完全な形状条件・組立・操作感・強度を検査していないため、全体unknownが正しい結果です。`examples/verified_fixture/`に入力STEPと出力レポートを添付しています。

## ビルド・インストール形式

`dist/cadmcp_design_brain-0.1.0-py3-none-any.whl`をビルドし、`pip install --no-deps --target ...`で別ディレクトリにインストールしました。ソースフォルダ外から、インストール先をPYTHONPATHに設定し、doctorとdemo --geometryが動くことを確認しました。wheel中のPython・データ・プロンプトが最終ソースと一致することも検査しました。

このwheel試験は**既存の検証環境の依存物を利用**しており、全依存をネットワークから取得するクリーンWindows環境の再現試験ではありません。wheelのsha256:

```text
6c64425cc199fefb1db237d007979dd8571ac8a3d5e411aaceddf716bd5065a2
```

## 実行環境

Python: `3.13.5`。OS: `Linux-6.18.44-x86_64-with-glibc2.41`。

| 依存 | 使用版 |
|---|---|
| pydantic | 2.13.4 |
| jsonschema | 4.26.0 |
| pytest | 9.0.2 |
| coverage | 7.13.3 |
| cadquery | 2.8.0 |
| setuptools | 82.0.1 |

statement coverage: **85.76%**（`cadmcp_brain`と`scripts`）。測定対象は親プロセスで、実際に起動したCLI／geometry worker等の子プロセス実行はcoverageへ合算していません。0%表示の子プロセスモジュールにも別プロセスの実行試験があります。一方、インストーラーのpip実行部やエラー分岐には未実行箇所もあります。coverageは安全性・意味理解・製品完成度の指標ではありません。

## 未実施・未保証

ユーザーの既存cadMCPソースへの統合、実AgentCAD・Fusion、実Windows、実Cursor/Codexクライアント、実PCBとスイッチの適合、自由文の意図理解ベンチマーク、実造形、疲労・クリープ・破断・操作感は未検証です。GitHub ActionsのWindows/Linuxマトリクスは設定を同梱しただけで、この会話から実行した結果ではありません。

同梱ソフトウェアは機能範囲を限定して実行確認した基盤です。**141件合格していても「どんな指示でも完璧に設計できる」という保証にはなりません。**

## 再実行

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m cadmcp_brain --workspace .\workspace demo --geometry
```

検証ログにある`/mnt/data/...`は作成時の環境パスです。ユーザーのMCP設定へコピーするパスではありません。設定は`setup.cmd`で自分のPC上に生成してください。
