# 動作試験報告 — v0.2.0 Req2CAD

日付: 2026-09-16。対象はこの配布コードです。

## 結果

**209件合格 / 0失敗 / 0エラー / 0skip。** 最終コードを重複しない3群に分けて実行しました。

| 試験群 | 合格 |
|---|---:|
| Req2CAD・型/ゲート・設計状態 | 153 |
| 既存ブリッジ・既存幾何・基本導入設定 | 42 |
| 既存MCPプロトコル・実プロセス通信 | 14 |
| 合計 | 209 |

根拠は `verification/*.xml` と同名ログ、`environment.json` です。異なる条件や重複実行を合算していません。途中の旧テストで、ツール数の固定期待値17を新しい24へ更新しました。試験を削除して通したわけではありません。

## Req2CAD追加部分で検査したこと

CSV原本hash、型、不正行、空機能、重複、同一再取込でCADを保持、atomicな失敗、exact UID、TARの危険パス、CADの改変、関数検索の複数機能対応、索引再開/世代不一致/改変、明示的な字句検索、偽の意味検索へ切り替えない動作を検査しました。

実CADカーネルでは、円・円弧・線のプロフィール、穴付き押出、片側/負方向/両側/対称押出、座標変換、union/cut/intersect、STEP再読込、4方向SVG、共有面edge、WLの番号付け不変性、形状比較を検査しました。未知操作を勝手に違う形へ置換しません。

参照根拠の偽digest、未実体化CAD、変更済みCADを拒否し、機構へ実参照を接続する設定も検査しました。物理単位が不明な場合はモデル座標として出力し、mm値だと誤表示しないことも確認しました。

**実MCP stdioプロセス**で `brain_fs_status/search/materialize/evidence` を呼び、CAD出力まで確認しました。入力は作者作成の試験データ、検索は明示的な字句検索です。

## 何を試していないか

**Qwen3-Embedding-4B本体の実ロード・推論は未実施です。** 意味検索の数値処理試験は、`FixtureEncoder` という明示的な制御ベクトルを返す試験器によります。実際の文章理解・日英検索精度・実モデルの互換性の証明ではありません。

**元CSV・元CAD全件の取得はこの環境で失敗しました。** 全175,978行/論文128,873件の実取込・フィルタ後件数・UID対応率・再構成成功率を測定したわけではありません。作者の試験片をその代用品として実データと称していません。

Windows実機、CUDA実推論、クリーン依存導入、実Cursor/Codex、実AgentCAD/Fusionへの書込、ユーザーの基板・スイッチ・シェル、印刷・組立・強度・疲労は未検証です。`setup_req2cad` のネットワーク全経路も未検証です。

## 実際の試験片

`examples/req2cad_fixture` に3つの作者作成形状と結果を保存しました。穴付き円筒では円筒面2、平面2、面隣接4が測定され、比較用の直方体は平面6です。両者のWL正規化類似度は0.1767766952966369でした。これはトポロジー比較の計算結果であり、構造の優劣や機能適合率ではありません。

デモの機能注釈は説明用です。円筒だから摩擦・復帰・耐久が実用条件を満たすとは判断しません。

## 再実行

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe scripts\demo_req2cad.py
```

環境はLinux / Python 3.13、CadQuery 2.8.0です。テスト数を『設計成功件数』『元データ処理成功数』『実機保証』と読み替えません。

## wheel配布形式の確認

0.2.0 wheelをビルドし、別ディレクトリへ `pip install --no-deps --target` で配置しました。ソースフォルダ外からそのモジュールを読み、24ツールの登録と作者作成CADの実worker出力を確認しました。既存環境の依存を使った検査であり、ネットワークから全依存を入れたクリーンWindows試験ではありません。ログは `verification/wheel-*.log` です。

SVG出力の試験片1件は白背景へ変換して表示を確認し、`preview_authored_fixture.png` として同梱しました。元Req2CAD部品ではありません。
