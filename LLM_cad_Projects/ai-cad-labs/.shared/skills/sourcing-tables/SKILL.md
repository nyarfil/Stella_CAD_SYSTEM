---
name: sourcing-tables
description: PROVISIONAL STUB (no content yet), a reserved slot for static component reference tables; bearing ID/OD/width tables, fastener catalogs, standard component dimensions for the sourcing agent. Use when selecting off-the-shelf components (bearings, fasteners, standard parts), but until content lands, this skill only tells you it is empty; do NOT treat it as an authoritative source.
---

# Sourcing Tables (reserved slot)

> [!warning] Provisional stub
> This is a reserved knowledge slot. Planned content (static component reference
> tables for the sourcing agent: bearing ID/OD/width tables, fastener catalogs, standard
> component dimensions) lands in a future release. Until then:
>
> - **Agents must NOT treat this skill as authoritative.** There is no content to cite.
> - If you need component dimensions NOW: use general engineering knowledge or the project's
>   `external/bom.md` notes, state your source/assumption explicitly, and flag the answer
>   as unbacked-by-catalog.
> - Do not delete this directory: the slot's existence is intentional (it keeps the
>   knowledge-layer layout stable so future content lands without re-wiring agents).
