Generate a rectangular shim bracket as a single parametric part.

Create a project part with the id `shim`. The bracket is a flat plate 60 mm
long, 24 mm wide and 4 mm thick, with R3 rounds on all four corners and one
central Ø8 mm through-hole on the plate's axis.

Give the part typed PARAMS over its tunable dimensions (length, width,
thickness, corner radius and bore diameter) and encode every stated constraint
as a design spec so that "done" is measurable.

Datum: the plate's bottom face lies on Z = 0 and its centre is at the origin,
with the 60 mm dimension along +X.
