# AppContainer probe — 2026-09-15

## Round 1 (fail)

`.venv\Scripts\python.exe` はランチャー。隔離の中ではユーザーフォルダ上の CPython が見えず、終了コード 103。

## Round 2 (fail)

CPython を `E:\...\ .python` に移し、本物の `python.exe` を直接起動。プロセスは立ち上がるが `0xC0000135` (`STATUS_DLL_NOT_FOUND`)。`(OI)(CI)RX` はフォルダ用で、`python312.dll` には SID が付かない。

## Round 3 (pass) — 28/28 OK

フォルダには継承 ACE、ファイルには `/T` で `RX`。加えて `PYTHONPATH` に venv の site-packages。

```
PROBE spawn OK ... TokenIsAppContainer=True
PROBE ping OK build123d=0.11.1
PROBE build OK ... triangles=12
PROBE export OK ... box.step
PROBE battery OK 7/7
PROBE summary 28/28 OK, 0 FAIL
```
