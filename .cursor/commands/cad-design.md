# 実例構造を使って設計する

Read `.cursor/skills/cadmcp-cursor/SKILL.md` and follow it for the user's request.
Retain the exact original requirements; first inspect registered hardware and
Req2CAD availability. Retrieve and inspect real reference CAD when it helps.
A Recipe needs real STEP (Req2CAD or a registered project STEP). Build a typed
prototype, inspect real measurements, and if assembling in Fusion use only the
issued `brain_fusion_handoff` script then `brain_fusion_ingest`. Perform
first-round plus peer-challenge review using available read-only Cursor
subagents. The parent submits actual returned reviews. No invented model
executions or fallback corpus. Do not run Codex or Cursor CLI from this command.
Keep canonical CAD untouched. Do not save f3d unless the customer asks.
